<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="edu.thu.xface" >

    <!-- android:versionCode="2" -->
    <!-- android:versionName="2.0" -->


    <!-- <uses-sdk -->
    <!-- android:minSdkVersion="9" -->
    <!-- android:targetSdkVersion="18" /> -->

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.CAMERA" />

    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.camera.autofocus"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.camera.front"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.camera.front.autofocus"
        android:required="false" />

    <!-- 屏幕为肖像模式 android:screenOrientation="portrait" -->
    <!-- android:configChanges="keyboardHidden|orientation" -->
    <application
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/android:Theme.NoTitleBar.Fullscreen" >
        <activity
            android:name=".LogoActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SignupActivity" />
        <activity android:name=".SignupCameraActivity" />
        <activity android:name=".FacerecCameraActivity" />
        <activity android:name=".AboutActivity" />
        <activity android:name=".SettingActivity" />
        <activity android:name=".UsersActivity" />
        <activity android:name=".UserpicsActivity" />
        <activity android:name=".TestActivity" />
    </application>

</manifest>
